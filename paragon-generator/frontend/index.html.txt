<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paragon Generator - Making Roblox Easier</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', sans-serif;
            background: #000;
            color: #fff;
            overflow-x: hidden;
        }

        /* Opening Animation */
        .opening-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #000;
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center;
            animation: fadeOut 1s ease 3s forwards;
        }

        .opening-text {
            font-size: 64px;
            font-weight: 700;
            letter-spacing: -2px;
            background: linear-gradient(135deg, #3b82f6, #8b5cf6, #ec4899);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            opacity: 0;
            animation: textFadeIn 2s ease forwards;
        }

        @keyframes textFadeIn {
            0% { opacity: 0; transform: translateY(20px); }
            20% { opacity: 1; transform: translateY(0); }
            80% { opacity: 1; transform: translateY(0); }
            100% { opacity: 0; transform: translateY(-20px); }
        }

        @keyframes fadeOut {
            to { opacity: 0; visibility: hidden; }
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            padding: 20px 60px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            z-index: 1000;
            opacity: 0;
            animation: navFadeIn 1s ease 3.5s forwards;
        }

        @keyframes navFadeIn {
            to { opacity: 1; }
        }

        .logo {
            font-size: 18px;
            font-weight: 600;
            letter-spacing: -0.5px;
        }

        .nav-sections {
            display: flex;
            gap: 60px;
            align-items: center;
        }

        .nav-label {
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: rgba(255, 255, 255, 0.4);
            margin-right: 30px;
        }

        .nav-links {
            display: flex;
            gap: 40px;
        }

        .nav-links a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            font-size: 15px;
            font-weight: 500;
            transition: color 0.3s;
            position: relative;
            cursor: pointer;
        }

        .nav-links a:hover,
        .nav-links a.active {
            color: #fff;
        }

        .nav-links a.active::after {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, #3b82f6, #8b5cf6);
        }

        .user-section {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .user-info {
            display: none;
            align-items: center;
            gap: 15px;
        }

        .user-info.logged-in {
            display: flex;
        }

        .user-avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            font-weight: 600;
        }

        .user-name {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.9);
        }

        .login-button,
        .logout-button {
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            color: #fff;
            padding: 10px 24px;
            border: none;
            border-radius: 50px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }

        .login-button:hover,
        .logout-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(59, 130, 246, 0.4);
        }

        .logout-button {
            background: rgba(255, 255, 255, 0.1);
            display: none;
        }

        .logout-button.logged-in {
            display: block;
        }

        /* Main Content */
        main {
            margin-top: 80px;
            opacity: 0;
            animation: contentFadeIn 1s ease 4s forwards;
        }

        @keyframes contentFadeIn {
            to { opacity: 1; }
        }

        .section {
            min-height: 100vh;
            padding: 100px 60px;
            display: none;
        }

        .section.active {
            display: block;
        }

        /* Login Required Overlay */
        .login-required {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            z-index: 999;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            gap: 30px;
        }

        .login-required.show {
            display: flex;
        }

        .login-required-icon {
            font-size: 80px;
        }

        .login-required-title {
            font-size: 36px;
            font-weight: 700;
        }

        .login-required-text {
            font-size: 18px;
            color: rgba(255, 255, 255, 0.6);
            text-align: center;
            max-width: 500px;
        }

        .login-required-button {
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            color: #fff;
            padding: 16px 40px;
            border: none;
            border-radius: 50px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            margin-top: 20px;
        }

        .login-required-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 50px rgba(59, 130, 246, 0.5);
        }

        /* AI Generator Section */
        .ai-generator {
            max-width: 1200px;
            margin: 0 auto;
        }

        .generator-title {
            font-size: 72px;
            font-weight: 700;
            letter-spacing: -3px;
            margin-bottom: 20px;
            text-align: center;
        }

        .generator-subtitle {
            font-size: 20px;
            color: rgba(255, 255, 255, 0.6);
            text-align: center;
            margin-bottom: 60px;
        }

        .ai-chat-container {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 24px;
            padding: 40px;
            min-height: 600px;
            display: flex;
            flex-direction: column;
        }

        .chat-messages {
            flex: 1;
            overflow-y: auto;
            margin-bottom: 30px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .chat-message {
            padding: 20px;
            border-radius: 16px;
            max-width: 80%;
            animation: slideIn 0.3s ease;
        }

        @keyframes slideIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .chat-message.user {
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            align-self: flex-end;
            margin-left: auto;
        }

        .chat-message.ai {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            align-self: flex-start;
        }

        .message-content {
            font-size: 15px;
            line-height: 1.6;
        }

        .message-time {
            font-size: 12px;
            opacity: 0.6;
            margin-top: 8px;
        }

        .chat-input-container {
            display: flex;
            gap: 15px;
        }

        .chat-input {
            flex: 1;
            background: rgba(0, 0, 0, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 50px;
            padding: 16px 24px;
            color: #fff;
            font-size: 15px;
            font-family: inherit;
            transition: all 0.3s;
        }

        .chat-input:focus {
            outline: none;
            border-color: #3b82f6;
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
        }

        .send-button {
            background: linear-gradient(135deg, #3b82f6, #8b5cf6);
            color: #fff;
            padding: 16px 32px;
            border: none;
            border-radius: 50px;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
            white-space: nowrap;
        }

        .send-button:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(59, 130, 246, 0.4);
        }

        .send-button:disabled {
            opacity: 0.5;
            cursor: not-allowed;
        }

        /* Projects Section */
        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 30px;
            max-width: 1400px;
            margin: 0 auto;
        }

        .project-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 30px;
            transition: all 0.4s;
            cursor: pointer;
            position: relative;
        }

        .project-card:hover {
            transform: translateY(-5px);
            border-color: rgba(59, 130, 246, 0.5);
            box-shadow: 0 20px 60px rgba(59, 130, 246, 0.3);
        }

        .project-header {
            display: flex;
            justify-content: space-between;
            align-items: start;
            margin-bottom: 15px;
        }

        .project-title {
            font-size: 20px;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .project-date {
            font-size: 12px;
            color: rgba(255, 255, 255, 0.5);
        }

        .project-prompt {
            color: rgba(255, 255, 255, 0.7);
            font-size: 14px;
            line-height: 1.6;
            margin-bottom: 20px;
            overflow: hidden;
            text-overflow: ellipsis;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
        }

        .project-actions {
            display: flex;
            gap: 10px;
        }

        .project-button {
            flex: 1;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: #fff;
            padding: 10px;
            border-radius: 8px;
            font-size: 13px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s;
        }

        .project-button:hover {
            background: rgba(255, 255, 255, 0.1);
            border-color: rgba(59, 130, 246, 0.5);
        }

        .delete-button {
            background: rgba(239, 68, 68, 0.1);
            border-color: rgba(239, 68, 68, 0.3);
        }

        .delete-button:hover {
            background: rgba(239, 68, 68, 0.2);
            border-color: rgba(239, 68, 68, 0.5);
        }

        .empty-state {
            text-align: center;
            padding: 100px 20px;
            color: rgba(255, 255, 255, 0.5);
        }

        .empty-icon {
            font-size: 64px;
            margin-bottom: 20px;
            opacity: 0.3;
        }

        .empty-text {
            font-size: 20px;
        }

        /* Loading Spinner */
        .loading {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            border-top-color: #fff;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* Success Toast */
        .toast {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: linear-gradient(135deg, #10b981, #059669);
            color: #fff;
            padding: 16px 24px;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(16, 185, 129, 0.4);
            display: none;
            align-items: center;
            gap: 12px;
            z-index: 10000;
            animation: slideUp 0.3s ease;
        }

        @keyframes slideUp {
            from { transform: translateY(100px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .toast.show {
            display: flex;
        }

        .toast-icon {
            font-size: 24px;
        }

        .toast-message {
            font-size: 15px;
            font-weight: 500;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .opening-text {
                font-size: 40px;
            }

            nav {
                padding: 20px 30px;
            }

            .nav-sections {
                gap: 20px;
            }

            .nav-label {
                display: none;
            }

            .section {
                padding: 80px 30px;
            }

            .generator-title {
                font-size: 48px;
            }

            .projects-grid {
                grid-template-columns: 1fr;
            }

            .chat-message {
                max-width: 90%;
            }
        }
    </style>
</head>
<body>
    <!-- Opening Animation -->
    <div class="opening-animation">
        <div class="opening-text">Making Roblox Easier</div>
    </div>

    <!-- Login Required Overlay -->
    <div class="login-required" id="loginRequired">
        <div class="login-required-icon">🔒</div>
        <h2 class="login-required-title">Login Required</h2>
        <p class="login-required-text">
            Please log in to use the AI Generator and access your projects.
        </p>
        
        <!-- Login/Register Form -->
        <div style="background: rgba(255,255,255,0.05); padding: 40px; border-radius: 20px; width: 400px; max-width: 90%;">
            <div id="loginForm">
                <input type="text" id="loginUsername" placeholder="Username" style="width: 100%; padding: 14px; border-radius: 10px; border: 1px solid rgba(255,255,255,0.2); background: rgba(0,0,0,0.5); color: #fff; font-size: 15px; margin-bottom: 15px; font-family: inherit;">
                <input type="password" id="loginPassword" placeholder="Password" style="width: 100%; padding: 14px; border-radius: 10px; border: 1px solid rgba(255,255,255,0.2); background: rgba(0,0,0,0.5); color: #fff; font-size: 15px; margin-bottom: 20px; font-family: inherit;">
                <button class="login-required-button" onclick="loginSubmit()" style="width: 100%;">
                    Login
                </button>
                <p style="text-align: center; margin-top: 20px; color: rgba(255,255,255,0.6); font-size: 14px;">
                    Don't have an account? <a href="#" onclick="showRegisterForm(); return false;" style="color: #3b82f6; text-decoration: none;">Register</a>
                </p>
                <p style="text-align: center; margin-top: 10px; color: rgba(255,255,255,0.5); font-size: 13px;">
                    Test account: <strong>TestUser</strong> / <strong>password123</strong>
                </p>
            </div>
            
            <div id="registerForm" style="display: none;">
                <input type="text" id="registerUsername" placeholder="Choose Username" style="width: 100%; padding: 14px; border-radius: 10px; border: 1px solid rgba(255,255,255,0.2); background: rgba(0,0,0,0.5); color: #fff; font-size: 15px; margin-bottom: 15px; font-family: inherit;">
                <input type="password" id="registerPassword" placeholder="Choose Password (6+ chars)" style="width: 100%; padding: 14px; border-radius: 10px; border: 1px solid rgba(255,255,255,0.2); background: rgba(0,0,0,0.5); color: #fff; font-size: 15px; margin-bottom: 20px; font-family: inherit;">
                <button class="login-required-button" onclick="registerSubmit()" style="width: 100%;">
                    Create Account
                </button>
                <p style="text-align: center; margin-top: 20px; color: rgba(255,255,255,0.6); font-size: 14px;">
                    Already have an account? <a href="#" onclick="showLoginForm(); return false;" style="color: #3b82f6; text-decoration: none;">Login</a>
                </p>
            </div>
        </div>
    </div>

    <!-- Navigation -->
    <nav>
        <div class="logo">Paragon Generator</div>
        <div class="nav-sections">
            <span class="nav-label">Generator</span>
            <div class="nav-links">
                <a href="#generator" class="nav-link active" data-section="generator" onclick="switchTab('generator')">Generator</a>
                <a href="#projects" class="nav-link" data-section="projects" onclick="switchTab('projects')">Projects</a>
            </div>
        </div>
        <div class="user-section">
            <button class="login-button" onclick="showLoginOverlay()">Login</button>
            <div class="user-info">
                <div class="user-avatar" id="userAvatar"></div>
                <span class="user-name" id="userName"></span>
            </div>
            <button class="logout-button" onclick="logout()">Logout</button>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        <!-- Generator Section -->
        <section id="generator" class="section ai-generator active">
            <h1 class="generator-title">AI Generator</h1>
            <p class="generator-subtitle">Describe what you want to build and let AI create it for you</p>

            <div class="ai-chat-container">
                <div class="chat-messages" id="chatMessages">
                    <div class="chat-message ai">
                        <div class="message-content">
                            👋 Hello! I'm your AI assistant. Describe the Roblox game or system you want to create, and I'll generate it for you!
                            <br><br>
                            For example: "Create a racing game with power-ups and lap tracking"
                        </div>
                    </div>
                </div>

                <div class="chat-input-container">
                    <input 
                        type="text" 
                        class="chat-input" 
                        id="chatInput"
                        placeholder="Describe your game or system..."
                        onkeypress="if(event.key==='Enter') sendMessage()"
                    >
                    <button class="send-button" id="sendButton" onclick="sendMessage()">
                        Generate
                    </button>
                </div>
            </div>
        </section>

        <!-- Projects Section -->
        <section id="projects" class="section">
            <h1 class="generator-title">Your Projects</h1>
            <p class="generator-subtitle">View and manage your AI-generated projects</p>

            <div class="projects-grid" id="projectsGrid">
                <!-- Projects will be loaded here -->
            </div>
        </section>
    </main>

    <!-- Success Toast -->
    <div class="toast" id="toast">
        <span class="toast-icon">✅</span>
        <span class="toast-message" id="toastMessage"></span>
    </div>

    <script>
        const API_URL = 'http://localhost:3000';
        let isAuthenticated = false;
        let currentUser = null;

        // Show/hide login forms
        function showLoginForm() {
            document.getElementById('loginForm').style.display = 'block';
            document.getElementById('registerForm').style.display = 'none';
        }

        function showRegisterForm() {
            document.getElementById('loginForm').style.display = 'none';
            document.getElementById('registerForm').style.display = 'block';
        }

        function showLoginOverlay() {
            document.getElementById('loginRequired').classList.add('show');
            showLoginForm();
        }

        // Login submit
        async function loginSubmit() {
            const username = document.getElementById('loginUsername').value.trim();
            const password = document.getElementById('loginPassword').value;

            if (!username || !password) {
                alert('Please enter username and password');
                return;
            }

            try {
                const response = await fetch(`${API_URL}/api/auth/login`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    credentials: 'include',
                    body: JSON.stringify({ username, password })
                });

                const data = await response.json();

                if (data.success) {
                    isAuthenticated = true;
                    currentUser = data;
                    updateUIForAuthState(true);
                    document.getElementById('loginRequired').classList.remove('show');
                    showToast('Welcome back, ' + data.username + '!');
                    
                    // Clear form
                    document.getElementById('loginUsername').value = '';
                    document.getElementById('loginPassword').value = '';
                } else {
                    alert(data.message || 'Login failed');
                }
            } catch (error) {
                console.error('Login error:', error);
                alert('Connection error! Make sure backend is running:\ncd backend-fixed && npm start');
            }
        }

        // Register submit
        async function registerSubmit() {
            const username = document.getElementById('registerUsername').value.trim();
            const password = document.getElementById('registerPassword').value;

            if (!username || !password) {
                alert('Please enter username and password');
                return;
            }

            if (username.length < 3) {
                alert('Username must be at least 3 characters');
                return;
            }

            if (password.length < 6) {
                alert('Password must be at least 6 characters');
                return;
            }

            try {
                const response = await fetch(`${API_URL}/api/auth/register`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    credentials: 'include',
                    body: JSON.stringify({ username, password })
                });

                const data = await response.json();

                if (data.success) {
                    isAuthenticated = true;
                    currentUser = data;
                    updateUIForAuthState(true);
                    document.getElementById('loginRequired').classList.remove('show');
                    showToast('Account created! Welcome, ' + data.username + '!');
                    
                    // Clear form
                    document.getElementById('registerUsername').value = '';
                    document.getElementById('registerPassword').value = '';
                } else {
                    alert(data.error || 'Registration failed');
                }
            } catch (error) {
                console.error('Register error:', error);
                alert('Connection error! Make sure backend is running:\ncd backend-fixed && npm start');
            }
        }

        // Check authentication status on load
        async function checkAuth() {
            try {
                const response = await fetch(`${API_URL}/api/auth/status`, {
                    credentials: 'include'
                });
                const data = await response.json();
                
                if (data.authenticated) {
                    isAuthenticated = true;
                    currentUser = data;
                    updateUIForAuthState(true);
                } else {
                    updateUIForAuthState(false);
                }
            } catch (error) {
                console.error('Auth check failed:', error);
                updateUIForAuthState(false);
            }
        }

        function updateUIForAuthState(authenticated) {
            const loginButton = document.querySelector('.login-button');
            const logoutButton = document.querySelector('.logout-button');
            const userInfo = document.querySelector('.user-info');
            const userAvatar = document.getElementById('userAvatar');
            const userName = document.getElementById('userName');

            if (authenticated) {
                loginButton.style.display = 'none';
                logoutButton.classList.add('logged-in');
                userInfo.classList.add('logged-in');
                userAvatar.textContent = currentUser.username[0].toUpperCase();
                userName.textContent = currentUser.username;
            } else {
                loginButton.style.display = 'block';
                logoutButton.classList.remove('logged-in');
                userInfo.classList.remove('logged-in');
            }
        }

        async function logout() {
            try {
                await fetch(`${API_URL}/api/auth/logout`, {
                    method: 'POST',
                    credentials: 'include'
                });
                isAuthenticated = false;
                currentUser = null;
                updateUIForAuthState(false);
                showToast('Logged out successfully');
                
                // Go back to generator tab if on projects
                switchTab('generator');
            } catch (error) {
                console.error('Logout failed:', error);
            }
        }

        function requireAuth() {
            if (!isAuthenticated) {
                document.getElementById('loginRequired').classList.add('show');
                return false;
            }
            document.getElementById('loginRequired').classList.remove('show');
            return true;
        }

        function switchTab(tab) {
            // Check auth for protected tabs
            if (tab === 'generator' || tab === 'projects') {
                if (!requireAuth()) return;
            }

            // Update nav links
            document.querySelectorAll('.nav-link').forEach(link => {
                link.classList.remove('active');
            });
            document.querySelector(`[data-section="${tab}"]`).classList.add('active');

            // Update sections
            document.querySelectorAll('.section').forEach(section => {
                section.classList.remove('active');
            });
            document.getElementById(tab).classList.add('active');

            // Load data for specific tabs
            if (tab === 'projects') {
                loadProjects();
            }
        }

        async function sendMessage() {
            if (!requireAuth()) return;

            const input = document.getElementById('chatInput');
            const sendButton = document.getElementById('sendButton');
            const messagesContainer = document.getElementById('chatMessages');
            
            const message = input.value.trim();
            if (!message) return;

            // Disable input
            input.disabled = true;
            sendButton.disabled = true;
            sendButton.innerHTML = '<span class="loading"></span>';

            // Add user message
            addMessage('user', message);
            input.value = '';

            try {
                const response = await fetch(`${API_URL}/api/generate`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    credentials: 'include',
                    body: JSON.stringify({
                        projectName: message.substring(0, 50),
                        prompt: message
                    })
                });

                const data = await response.json();

                if (data.success) {
                    // Add AI response
                    addMessage('ai', data.aiResponse);
                    showToast('Project generated! Open Studio and click Connect');
                } else {
                    addMessage('ai', `❌ ${data.error || 'Generation failed. Please try again.'}`);
                }
            } catch (error) {
                console.error('Generation error:', error);
                addMessage('ai', '❌ Connection error. Make sure the backend is running.');
            }

            // Re-enable input
            input.disabled = false;
            sendButton.disabled = false;
            sendButton.textContent = 'Generate';
            input.focus();
        }

        function addMessage(type, content) {
            const messagesContainer = document.getElementById('chatMessages');
            const messageDiv = document.createElement('div');
            messageDiv.className = `chat-message ${type}`;
            
            const now = new Date();
            const time = now.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' });
            
            messageDiv.innerHTML = `
                <div class="message-content">${escapeHtml(content)}</div>
                <div class="message-time">${time}</div>
            `;
            
            messagesContainer.appendChild(messageDiv);
            messagesContainer.scrollTop = messagesContainer.scrollHeight;
        }

        async function loadProjects() {
            if (!requireAuth()) return;

            const projectsGrid = document.getElementById('projectsGrid');
            projectsGrid.innerHTML = '<div class="empty-state"><div class="loading"></div></div>';

            try {
                const response = await fetch(`${API_URL}/api/projects`, {
                    credentials: 'include'
                });
                const data = await response.json();

                if (data.projects.length === 0) {
                    projectsGrid.innerHTML = `
                        <div class="empty-state">
                            <div class="empty-icon">📁</div>
                            <div class="empty-text">No projects yet. Create your first project in the Generator!</div>
                        </div>
                    `;
                    return;
                }

                projectsGrid.innerHTML = '';
                data.projects.forEach(project => {
                    const card = createProjectCard(project);
                    projectsGrid.appendChild(card);
                });
            } catch (error) {
                console.error('Failed to load projects:', error);
                projectsGrid.innerHTML = `
                    <div class="empty-state">
                        <div class="empty-icon">❌</div>
                        <div class="empty-text">Failed to load projects</div>
                    </div>
                `;
            }
        }

        function createProjectCard(project) {
            const card = document.createElement('div');
            card.className = 'project-card';
            
            const date = new Date(project.createdAt);
            const dateStr = date.toLocaleDateString('en-US', { 
                month: 'short', 
                day: 'numeric',
                year: 'numeric'
            });
            
            card.innerHTML = `
                <div class="project-header">
                    <div>
                        <div class="project-title">${escapeHtml(project.name)}</div>
                        <div class="project-date">${dateStr}</div>
                    </div>
                </div>
                <div class="project-prompt">${escapeHtml(project.prompt)}</div>
                <div class="project-actions">
                    <button class="project-button" onclick="viewProject('${project.id}')">View Details</button>
                    <button class="project-button delete-button" onclick="deleteProject('${project.id}')">Delete</button>
                </div>
            `;
            
            return card;
        }

        function escapeHtml(text) {
            const div = document.createElement('div');
            div.textContent = text;
            return div.innerHTML;
        }

        async function deleteProject(projectId) {
            if (!confirm('Are you sure you want to delete this project?')) return;

            try {
                await fetch(`${API_URL}/api/projects/${projectId}`, {
                    method: 'DELETE',
                    credentials: 'include'
                });
                showToast('Project deleted');
                loadProjects();
            } catch (error) {
                console.error('Delete failed:', error);
                alert('Failed to delete project');
            }
        }

        function viewProject(projectId) {
            showToast('Viewing project details...');
            // You can expand this to show full project details
        }

        function showToast(message) {
            const toast = document.getElementById('toast');
            const toastMessage = document.getElementById('toastMessage');
            toastMessage.textContent = message;
            toast.classList.add('show');
            setTimeout(() => {
                toast.classList.remove('show');
            }, 3000);
        }

        // Allow Enter key to submit
        document.addEventListener('DOMContentLoaded', function() {
            document.getElementById('loginPassword')?.addEventListener('keypress', function(e) {
                if (e.key === 'Enter') loginSubmit();
            });
            document.getElementById('registerPassword')?.addEventListener('keypress', function(e) {
                if (e.key === 'Enter') registerSubmit();
            });
        });

        // Remove opening animation
        setTimeout(() => {
            document.querySelector('.opening-animation')?.remove();
        }, 4000);

        // Initialize
        checkAuth();
    </script>
</body>
</html>